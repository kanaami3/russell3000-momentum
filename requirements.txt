yfinance>=0.2.40
pandas>=2.0.0
requests>=2.31.0
anthropic>=0.40.0
openpyxl>=3.1.0
xlrd>=2.0.1
# curl_cffi enables browser-impersonated requests so yfinance Ticker.info /
# earnings_dates / quarterly_* endpoints don't get rate-limited on GitHub
# Actions IPs. yfinance auto-detects and uses it when installed.
curl_cffi>=0.6.0
lxml>=5.0.0
